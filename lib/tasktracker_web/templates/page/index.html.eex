<h1>Welcome to Task tracking app</h1>
    <!-- session_path(@conn, :create) -->
    <%= if @current_user do  %>
    <div>You are logged in.</div>
    <p><%= link "Task Page", to: page_path(@conn, :feed, user_id: @current_user.id) %></p>
    <%= if @current_user.manager do  %>
    <p><%= link "Managee Task Page", to: page_path(@conn, :managee_feed, user_id: @current_user.id) %></p>
    <% end %>
    <% else %>
    <h1>Log In</h1>
    <%= form_tag("/session", method: :post) do %>
      <div class="form-group">
        Email
        <input type="email" name="email" placeholder="user@example.com" >
      </div>
      <!-- passwords come later -->
      <div class="form-group">
        <button class="btn btn-primary">Log in</button>
        <%= link "Register", to: user_path(@conn, :new) %>
      </div>
    <% end %>
    <% end %>